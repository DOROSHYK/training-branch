// Пиши код ниже этой строки
function findMatches(matchNum, ...args) {
  const matches = []; // Не изменяй эту строку
  
  for (const arg of args) {
    console.log(arg);


  }
  
     
 
}
  // Пиши код выше этой строки
    
    

findMatches([1, 2, 3, 4, 5], 1, 8, 2, 7);